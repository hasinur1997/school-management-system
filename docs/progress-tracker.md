# Progress Tracker & Task Board

> The unit of work is one task below. **Every task has a full ticket file in `docs/tasks/` — read the ticket before implementing; the line here is only a pointer.** One task = one sitting: implementable and verifiable (`php artisan test` green) in a single session. Claude Code marks `[x]` only when the task's code AND tests are complete. Never start a task before all tasks above it in the same phase are done; never start a phase before the previous phase is done.

## Phase 1 — Foundation `[x]`
Specs: `api/auth.md`, `api/academic-structure.md`

- [x] [1.1](tasks/task-1.1-authentication.md) — Sanctum setup
- [x] [1.2](tasks/task-1.2-roles-permissions.md) — spatie/laravel-permission setup
- [x] [1.3](tasks/task-1.3-branches.md) — `branches` migration + model + CRUD (super admin only)
- [x] [1.4](tasks/task-1.4-academic-sessions.md) — `academic_sessions` migration + CRUD
- [x] [1.5](tasks/task-1.5-classes-sections.md) — `school_classes` + `sections` migrations + CRUD
- [x] [1.6](tasks/task-1.6-subjects.md) — `subjects` migration + CRUD
- [x] [1.7](tasks/task-1.7-branch-scope.md) — `BranchScope` global scope + `BelongsToBranch` trait (auto-stamp branch_id on create)
- [x] [1.8](tasks/task-1.8-teacher-assignments.md) — `teacher_assignments` migration + CRUD + filters

## Phase 2 — Teacher Management `[x]`
Specs: `api/teachers.md`

- [x] [2.1](tasks/task-2.1-teacher-create.md) — `teachers` migration + model
- [x] [2.2](tasks/task-2.2-credentials-job.md) — `SendCredentials` queued mail job (dispatch after commit)
- [x] [2.3](tasks/task-2.3-teacher-endpoints.md) — Teacher list/show/update/status endpoints

## Phase 3 — Admissions `[x]`
Specs: `api/admissions.md`

- [x] [3.1](tasks/task-3.1-admission-tables.md) — `admission_applications` + `admission_previous_educations` migrations + models (all bilingual fields per schema)
- [x] [3.2](tasks/task-3.2-student-tables.md) — Schema-only migrations for `students`, `parents`, `parent_student`, `enrollments` (needed by 3.5)
- [x] [3.3](tasks/task-3.3-public-admission.md) — Public submission endpoint
- [x] [3.4](tasks/task-3.4-admission-review.md) — Admin list/show with status/class/search filters
- [x] [3.5](tasks/task-3.5-admission-approve-reject.md) — Approve flow — one transaction

## Phase 4 — Students & Parents `[x]`
Specs: `api/students.md`

- [x] [4.1](tasks/task-4.1-student-endpoints.md) — Student models/relationships
- [x] [4.2](tasks/task-4.2-parents.md) — Parents CRUD + link/unlink endpoints + `/me/students`
- [x] [4.3](tasks/task-4.3-enrollment-history.md) — `/students/{id}/enrollments` history endpoint

## Phase 5 — Student Attendance `[x]`
Specs: `api/student-attendance.md`

- [x] [5.1](tasks/task-5.1-attendance-sheet.md) — `student_attendances` migration
- [x] [5.2](tasks/task-5.2-attendance-save.md) — POST bulk save
- [x] [5.3](tasks/task-5.3-attendance-monthly.md) — Monthly sheets

## Phase 6 — Teacher Attendance `[ ]`
Specs: `api/teacher-attendance.md`

- [x] [6.1](tasks/task-6.1-checkin-whitelist.md) — `teacher_attendances` + `checkin_ip_whitelists` migrations
- [x] [6.2](tasks/task-6.2-checkin.md) — Check-in/check-out
- [x] [6.3](tasks/task-6.3-teacher-attendance-admin.md) — Admin browse + correction (corrected_by) + `/me/teacher-attendance`

## Phase 7 — Exams & Marks `[x]`
Specs: `api/exams-marks.md`

- [x] [7.1](tasks/task-7.1-grading-scale.md) — `grading_scales` migration + Bangladesh-standard seeder
- [x] [7.2](tasks/task-7.2-exams.md) — `exams` migration + CRUD
- [x] [7.3](tasks/task-7.3-marks-entry.md) — `marks` migration

## Phase 8 — Results `[ ]`
Specs: `api/results.md`

- [x] [8.1](tasks/task-8.1-exam-results.md) — `exam_results` migration
- [x] [8.2](tasks/task-8.2-annual-results.md) — `annual_results` migration
- [x] [8.3](tasks/task-8.3-result-reads.md) — Result search + `/enrollments/{id}/results` + `/me/results`
- [ ] [8.4](tasks/task-8.4-result-pdfs.md) — Marksheet PDFs (per-exam + annual) via dompdf, streamed

## Phase 9 — Promotion `[ ]`
Specs: `api/promotions.md`

- [x] [9.1](tasks/task-9.1-promotion-preview.md) — `promotions` migration
- [x] [9.2](tasks/task-9.2-bulk-promotion.md) — Bulk promotion
- [x] [9.3](tasks/task-9.3-individual-promotion.md) — Individual promotion + override permission

## Phase 10 — Fees & Payments `[x]`
Specs: `api/fees-payments.md`

- [x] [10.1](tasks/task-10.1-fee-structures.md) — `fee_structures` migration + CRUD
- [x] [10.2](tasks/task-10.2-invoices.md) — `invoices` migration
- [x] [10.3](tasks/task-10.3-local-payment.md) — Local payment
- [x] [10.4](tasks/task-10.4-sslcommerz-init.md) — `PaymentGatewayService` wrapping SSLCommerz (faked in tests)
- [x] [10.5](tasks/task-10.5-sslcommerz-ipn.md) — IPN handler

## Phase 11 — Finance `[x]`
Specs: `api/finance.md`

- [x] [11.1](tasks/task-11.1-categories.md) — `categories` migration + CRUD (type income|expense)
- [x] [11.2](tasks/task-11.2-incomes.md) — `incomes` CRUD
- [x] [11.3](tasks/task-11.3-expenses.md) — `expenses` CRUD
- [x] [11.4](tasks/task-11.4-assets.md) — `assets` CRUD + summary endpoint (total value, by status)

## Phase 12 — Documents `[x]`
Specs: `api/documents.md`

- [x] [12.1](tasks/task-12.1-idcard-single.md) — Single ID card PDF (on demand, streamed)
- [x] [12.2](tasks/task-12.2-idcard-batch.md) — Batch ID card queued job (chunked merge) + poll endpoint (202/processing/done+url)
- [x] [12.3](tasks/task-12.3-tc.md) — TC

## Phase 13 — Reports `[ ]`
Specs: `api/reports.md`

- [ ] [13.1](tasks/task-13.1-report-filters.md) — Shared filter resolver
- [ ] [13.2](tasks/task-13.2-finance-reports.md) — Income / expense / profit-loss reports
- [ ] [13.3](tasks/task-13.3-entity-reports.md) — Students / teachers / assets / fees reports
- [ ] [13.4](tasks/task-13.4-report-pdfs.md) — Report PDF exports

## Phase 14 — Settings, Dashboard & Polish `[ ]`
Specs: `api/settings.md`

- [ ] [14.1](tasks/task-14.1-settings.md) — `settings` migration
- [ ] [14.2](tasks/task-14.2-dashboard.md) — Role-aware dashboard endpoint
- [ ] [14.3](tasks/task-14.3-demo-seeders.md) — Full demo seeders + factory review (one branch fully populated)
- [ ] [14.4](tasks/task-14.4-final-polish.md) — Final pass

---

## Open Questions

| # | Question | Raised during | Resolution |
|---|---|---|---|
| 1 | Roll numbers: confirmed per class/section/session (reassigned yearly)? | schema design | pending user confirmation |
| 2 | SMS provider for credential/payment notifications (which gateway)? | — | pending |
| 3 | Late fee rules (amount/percent, grace period) — toggle exists, behavior undefined | settings design | pending; do not implement until defined |
| 4 | Grading scale uses absolute `min_marks`/`max_marks` (0–100), but subjects may have `full_marks ≠ 100` — is grade mapping on raw marks or percentage? | docs readiness review | pending; needed before Phase 7 (marks entry) |
| 5 | `admission_applications.birth_reg_no` is unique — a rejected applicant cannot re-apply (e.g., next session) with the same birth registration no. Allow re-application by scoping uniqueness to non-rejected applications (app-level), or keep the hard block? | schema verification | **resolved 2026-06-13 (user):** keep the hard block — plain DB unique index on `birth_reg_no`, exactly as `database-schema.md` states. Re-application after rejection with the same birth reg no is not supported. |

## Decisions Log

- 2026-06-11 — Multi-branch confirmed; branch scoping via global scope.
- 2026-06-11 — PDFs on demand except TC (persisted, legal record).
- 2026-06-11 — Full-month payments default; partial behind setting toggle.
- 2026-06-11 — API spec split: index + per-module files matching build phases.
- 2026-06-11 — Execution model: module-based phases, task-based work units (this board).
- 2026-06-12 — Task 1.1: `users.branch_id` is created as an indexed nullable bigint **without** the FK constraint, because `branches` doesn't exist yet — the `create_branches_table` migration (Task 1.3) must add `foreign('branch_id')->references('id')->on('branches')` to `users`.
- 2026-06-12 — Task 1.1: API exception envelope handled centrally in `bootstrap/app.php` render closures (401/403/404/422 + generic HttpException incl. 429); success envelope via `App\Http\Controllers\Api\ApiController::success()`. Later tasks reuse both.
- 2026-06-12 — Task 1.2: `super_admin` role holds **zero** explicit permissions; bypass is `Gate::before` (`User::isSuperAdmin()`) in `AuthServiceProvider`. `UserResource` reports *effective* permissions, so super admins get the full permission list in `/auth/me` per the ticket's contract example.
- 2026-06-12 — Task 1.2: spatie's `UnauthorizedException` gets a dedicated render closure in `bootstrap/app.php` (registered **before** the generic `HttpExceptionInterface` closure — render closures match in registration order) so `permission:` middleware failures return the standard `{"success": false, "message": "This action is unauthorized."}` 403 envelope instead of spatie's default message.
- 2026-06-12 — Task 1.2: role bundle judgment calls (ticket said "sensible bundles"): admin additionally got `session.manage`, `class.manage`, `subject.manage` (academic structure is admin work; only branch CRUD is super-admin-only per the board), plus exam/attendance/teacher_attendance/result permissions, but **not** `marks.entry` (teachers enter marks), `branch.manage`, `setting.manage`, or finance (`income.manage`/`expense.manage`/`asset.manage` stay accountant-only). Accountant got `invoice.view` on top of the literal "finance + fee.collect + report.view" (collecting fees requires seeing invoices). Bundles live in `RoleSeeder::ROLES`; permission list in `PermissionSeeder::PERMISSIONS`.
- 2026-06-12 — Task 1.3: `branches` now owns the deferred FK for `users.branch_id`; branch deletes rely on DB restrict constraints and the API maps that failure to the required 409 envelope. `BranchSeeder` uses the codes `MP` and `JA` for the two known branches.
- 2026-06-12 — Task 1.4: model is `AcademicSession` (matches table, avoids clashing with framework `Session`); HTTP layer uses `Session*` names (`SessionController`, `SessionResource`, `Session/*Requests`) matching the `/sessions` endpoint and the ticket-mandated `SessionService`.
- 2026-06-12 — Task 1.4: "exactly one current session after any write" required two behaviors the ticket implied but didn't spell out: (a) the **first** session created always becomes current — explicit `is_current: false` with no current session existing is rejected 422 (`errors.is_current`, "One session must be current."), mirroring the documented update rule; (b) deleting the current session is rejected 422 with the same message (delete is a write too).
- 2026-06-12 — Task 1.4: the delete-in-use 409 depends on restrict FKs from `enrollments`/`exams`/`fee_structures`, none of which exist yet — those migrations (Phases 3/7/10) **must** declare `restrictOnDelete` FKs to `academic_sessions`. The required test exercises the path via a synthetic restrict-FK table created inside the test.
- 2026-06-13 — Task 1.5: Form Requests live in `App\Http\Requests\SchoolClass` because `Class` is a PHP reserved word (invalid namespace segment); model is `SchoolClass` per the table, HTTP layer uses `Class*` names (`ClassController`, `ClassResource`).
- 2026-06-13 — Task 1.5: `GET /classes` is an unpaginated dropdown list (≤12 rows/branch, same call as 1.4 made for sessions): active classes only, ordered by `numeric_level`, sections nested. Per the spec's "cached server-side" note it is cached via `Cache::remember` (key `academic.classes.{branch_id|all}`, 1h TTL); every class/section write in `ClassService` forgets the touched branch key plus the cross-branch key.
- 2026-06-13 — Task 1.5: branch handling until BranchScope (1.7): super admins **must** send `branch_id` on class create (they have no branch of their own) and may filter the index with `?branch_id=`; for everyone else `branch_id` input is `prohibited` (422) and stamping uses the caller's branch. Out-of-branch class/section access by non-super-admins returns 404 via explicit `assertClassVisibleTo`/`assertSectionVisibleTo` checks in `ClassService` — 1.7 replaces these with the global scope.
- 2026-06-13 — Task 1.5: `sections.class_teacher_id` is a plain nullable `unsignedBigInteger` (no FK — `teachers` doesn't exist); **Task 2.1 must add the FK**. Section delete-in-use 409 depends on future restrict FKs to `sections` (`teacher_assignments` 1.8, `enrollments` 3.2) — tested via a synthetic restrict-FK table, mirroring 1.4. Class delete-in-use already 409s for real via the `sections.class_id` restrict FK.
- 2026-06-13 — Task 1.6: the ticket-mandated `AcademicStructureService` now owns the academic-structure cache: the class-list caching from 1.5 moved into it from `ClassService` (keys unchanged, `academic.classes.{branch_id|all}`; `ClassService` delegates), and it holds the subject CRUD + cached subject reads. Subject lists are cached **per class** (`academic.subjects.{class_id}`) rather than per branch — the endpoint is class-nested, so per-class keys make invalidation exact; a stale key can't outlive its class because `subjects.class_id` restricts class deletes.
- 2026-06-13 — Task 1.6: `full_marks`/`pass_marks` are optional input (DB defaults 100/33); `pass_marks < full_marks` is enforced in the Form Requests' `after()` hooks against the *effective* values — input falling back to defaults on create and to stored values on update, so partial updates (e.g. raising only `pass_marks`) are checked against the stored `full_marks`. Error lands on `errors.pass_marks` per the ticket contract.
- 2026-06-13 — Task 1.7: `branch_id` input semantics changed from 1.5's `prohibited` (422) to **silently ignored** for non-super-admins, per the 1.7 contract ("Create as branch-1 admin ignores any submitted branch_id and stamps 1"): the class Form Requests use the `exclude` rule and `BelongsToBranch::creating` force-stamps `branch_id` from the auth user. `ClassSectionCrudTest::test_branch_id_is_super_admin_only_input` updated accordingly.
- 2026-06-13 — Task 1.7: `sections`/`subjects` carry no `branch_id` of their own, so the `BelongsToBranch` trait can't apply; isolation comes from a sibling `App\Models\Concerns\BelongsToBranchThroughClass` trait whose global scope adds `whereHas('schoolClass')` (the relation query carries `BranchScope`), making out-of-branch rows model-not-found at route binding. The 1.5/1.6 `assert*VisibleTo` helpers are removed from `ClassService` and controllers.
- 2026-06-13 — Task 1.7: scope-based binding 404s surfaced a latent envelope bug — Laravel prepares `ModelNotFoundException` into `NotFoundHttpException` **before** render closures run, so 1.1's `ModelNotFoundException` closure never matched and "No query results for model …" leaked through the generic `HttpExceptionInterface` closure. The closure now matches `NotFoundHttpException|ModelNotFoundException` and always returns the `Resource not found.` envelope.
- 2026-06-13 — Task 1.7: super admin list filter formalized in `ListClassesRequest` (`branch_id` = existing id, or `all`/omitted for every branch; invalid id → 422); the explicit `where('branch_id', …)` for that filter in `AcademicStructureService::listClasses` is the one sanctioned manual branch clause (documented in code). A branchless non-super-admin now gets an **uncached** empty list — caching it would land under the cross-branch `all` cache key (`classListKey(null)`).
- 2026-06-13 — Task 1.8: `teacher_assignments` created with an unconstrained `teacher_id` (no FK — teachers arrives in 2.1, which **must** add `foreign('teacher_id')->references('id')->on('teachers')`); `session_id`/`class_id`/`section_id`/`subject_id` are restrict FKs. DB `unique(teacher_id, session_id, class_id, section_id, subject_id)` defends only the fully-populated tuple — SQL treats NULLs as distinct, so duplicates with NULL `section_id`/`subject_id` (class duties) are caught by `TeacherAssignmentRequest` validation instead.
- 2026-06-13 — Task 1.8: model carries no `branch_id`; branch isolation reuses `BelongsToBranchThroughClass` via its `schoolClass()` relation (same pattern as sections/subjects). Body-supplied `class_id`/`section_id`/`subject_id` are resolved through their (branch-scoped) models in the Form Request `after()` hook, so out-of-branch ids report 422 `errors.class_id` rather than leaking other branches' rows; `session_id` uses a plain `exists` rule (sessions are global, no branch_id). Duplicate-tuple violations attach to `errors.teacher_id`.
- 2026-06-13 — Task 1.8: per ticket decision, all five CRUD endpoints are guarded by `permission:teacher.update` (no separate read permission — assignments are admin-managed). The `teacher()` relation + nested teacher name in `TeacherAssignmentResource` are deferred to 2.1; today the resource nests class/session/section/subject names (all eager loaded — N+1 guard test runs under `Model::shouldBeStrict()`).
- 2026-06-13 — Task 2.1: the deferred FKs landed here — `teacher_assignments.teacher_id` (`restrictOnDelete`) and `sections.class_teacher_id` (`nullOnDelete`, so deleting a class teacher just unsets the section). The new `teacher_id` FK broke the 1.8 tests/factory that used arbitrary integer teacher ids: `TeacherAssignmentFactory` now defaults `teacher_id` to `Teacher::factory()`, `TeacherFactory.branch_id` defaults to `Branch::factory()` (column is NOT NULL), and `TeacherAssignmentCrudTest::setUp` seeds teachers with the literal ids (4, 7) those tests reference. The 1.8-deferred `TeacherAssignment::teacher()` relation was added (resource/nested name still deferred to 2.3).
- 2026-06-13 — Task 2.1: `POST /teachers` creates the login (`users` row, random `Str::password(10)`, teacher role, creator's branch) + `teachers` profile in one `DB::transaction`; `SendCredentials` is dispatched `->afterCommit()` (verified to fire under RefreshDatabase). `SendCredentials` is a queued stub (empty `handle`, real mail in 2.2) carrying `User` + plaintext password with `tries=3`/`backoff=30`. Email/phone uniqueness is validated against the **users** table (login credentials) — duplicate phone surfaces there since `teachers.phone` is not unique. `teachers.status` uses new `App\Enums\TeacherStatus` (active/inactive). The login test had to call `$this->app['auth']->forgetGuards()` between the two authenticated requests because the sanctum guard caches the resolved user across requests within a single test.
- 2026-06-13 — Task 2.3: first `spatie/laravel-medialibrary` usage — installed `^11.23`, published `media` migration + config unedited; `Teacher implements HasMedia` with a single-file `photo` collection (`acceptsMimeTypes` jpg/png). `photo_url` resolves via `Teacher::photoUrl()` (`getFirstMediaUrl('photo') ?: null`); all reads eager-load `media` to dodge N+1 under `shouldBeStrict()`. **Decisions the ticket left open:** (a) `GET /teachers` defaults `sort=name asc` (joining_date defaults desc), filters `status`+`search` (LIKE across name/email/phone/designation), `per_page` capped 100 — same meta envelope as 1.8; (b) `PUT /teachers/{id}` mirrors `name`+`phone` onto the linked `users` row (phone is a login identifier, so the profile and login must not drift) and `email` is `prohibited` (422) since it's the login identity — phone uniqueness validated on `users` ignoring the teacher's own user; (c) `PATCH /teachers/{id}/status`: inactive sets `users.is_active=false` **and** deletes all tokens (active session cut immediately), active re-enables the login; (d) `POST /teachers/{id}/photo` replaces via the single-file collection. All `{teacher}` routes 404 out-of-branch via `BranchScope` route-model binding. `assignments` is only serialized on `show`, eager-loaded for the **current** session (`AcademicSession.is_current`) with class/section/subject names.
- 2026-06-13 — Task 2.2: `SendCredentials` now sends `CredentialsMail` (markdown view `resources/views/mail/credentials.blade.php`); gained a third ctor arg `string $role = 'Teacher'` so students/parents reuse it in 3.5, and `backoff` changed from int `30` to escalating `[60, 300]` per ticket. Login identifier in the mail is the user's **email** (login accepts email or phone; email is the stable credential); login URL hint is `config('app.url').'/login'`. `POST /teachers/{teacher}/resend-credentials` (`permission:teacher.create`) regenerates the password, `forceFill`s the new hash, revokes **all** tokens (`$user->tokens()->delete()`), and dispatches the job after commit — inactive teacher → 409 `"Teacher is inactive."` via `abort()` in the service, out-of-branch → 404 via route-model binding under `BranchScope`. Queue (`database`) + `failed_jobs` were already migrated (default `0001_01_01_000002_create_jobs_table`), so no new migration. Mail-sent assertions pass under the test `sync` queue, confirming `afterCommit` fires there.
- 2026-06-13 — Task 3.1 (open question #5 resolved by user): `admission_applications.birth_reg_no` keeps the **hard** DB unique index per the schema — a rejected applicant cannot re-apply with the same birth registration number. No app-level scoping. `application_no` and `birth_reg_no` uniqueness asserted via `QueryException` (SQLSTATE 23000) in tests.
- 2026-06-13 — Task 3.1: `AdmissionPreviousEducation` sets `$table = 'admission_previous_educations'` explicitly — "education" is uncountable, so Eloquent's inferred plural (`admission_previous_education`) was wrong. FK `application_id` is `cascadeOnDelete` (the only cascade in this module, per schema's "cascade only on pure child rows"); deleting an application removes its previous-education rows (tested).
- 2026-06-13 — Task 3.1: FK delete behavior judgment calls — `branch_id` and `desired_class_id` use `restrictOnDelete` (the schema's documented default). `reviewed_by` (nullable audit pointer → users) uses `nullOnDelete` so deleting a former reviewer just unsets the field rather than blocking the delete.
- 2026-06-13 — Task 3.1: `ApplicationNoGenerator` (`APP-{branchCode}-{seq}`, zero-padded to 5 digits, e.g. `APP-MP-00001`) is race-safe via a `Branch::lockForUpdate()` held for the enclosing `DB::transaction`, serializing concurrent same-branch submissions; sequence derived from the latest committed `application_no` (`orderByDesc('id')`, `withoutGlobalScope(BranchScope)`), with the DB unique index as the final guard. Sequences are independent per branch. Caller (3.3) should invoke `generate()` inside its create transaction so the lock spans the insert.
- 2026-06-13 — Task 3.2: `parents` carries its own `branch_id` per the schema, so `ParentProfile` uses `BelongsToBranch` (auto-stamp + scope) like `Student` — the ticket only spelled this out for `Student`. Model named `ParentProfile` (table `parents`) to dodge the PHP `parent` keyword clash; relations are `parents()`/`students()` (not `parent()`). `Enrollment` has **no** `branch_id` (derives via student per schema §41), so it gets no branch trait — isolation in Phase 4 will come through the `student` relation.
- 2026-06-13 — Task 3.2: FK delete behavior — `parent_student` is the only cascade (both `parent_id` and `student_id` `cascadeOnDelete`, per schema's "cascade only on pure child rows"); everything else `restrictOnDelete`. `enrollments.session_id` and `enrollments.section_id` are the real restrict FKs the synthetic-table tests in Tasks 1.4/1.5 anticipated. `students.application_id` is nullable + unique + `restrictOnDelete`.
- 2026-06-13 — Task 3.2: `Student::currentEnrollment()` is a `hasOne` filtered by `whereHas('session', is_current=true)` — resolves the enrollment whose academic session is the current one (not by enrollment `status`, since multiple sessions can have `active`-status rows historically). `AdmissionNoGenerator` mirrors `ApplicationNoGenerator` (branch `lockForUpdate` for race safety) but the sequence is scoped per branch **and** per year (`STU-{code}-{year}-{seq}`), derived from the latest `admission_no` matching the `STU-{code}-{year}-` prefix; `withTrashed()` included so soft-deleted students don't free up a number. Default year is `now()->year`; caller (3.5) passes the admission year explicitly.
- 2026-06-13 — Task 3.3: public endpoints live under a no-auth `v1/public/*` group; submission is `throttle:10,60` (10/hour/IP — the throttle middleware runs before validation, so even malformed requests count, which is how the 429 test reaches the limit). Controller is `PublicAdmissionController` (named to leave `AdmissionController` free for the admin endpoints in 3.4). In the public context `BranchScope` is bypassed and `BelongsToBranch` does **not** stamp `branch_id`, so `AdmissionService::submit` sets it explicitly from the validated input; `ApplicationNoGenerator::generate` is called inside the submit transaction so its branch `lockForUpdate` spans the insert. Photo + documents are added to medialibrary **inside** the transaction so a disk failure rolls back the application + previous-education rows (asserted by pointing the media disk at an undefined disk name).
- 2026-06-13 — Task 3.3 (surprise): the 3.1 migration made `admission_applications.mother_mobile` NOT NULL, but the ticket's API contract marks the input field `mother_mobile?` optional. Reconciled by validating it `nullable` and coalescing a missing value to `''` in the service (the schema doc, line 173, does not list it as nullable, so the NOT NULL column stands). Validation rules mirror the schema's bilingual/optional columns; `desired_class_id` is validated via `Rule::exists` scoped to `is_active` **and** the submitted `branch_id`, so an out-of-branch or inactive class reports 422 `errors.desired_class_id`; duplicate `birth_reg_no` reports the contract's exact message via `messages()`. Status lookup uses `whereDate('date_of_birth', …)->firstOrFail()` → 404 on any miss (never reveals existence).
- 2026-06-13 — Task 3.4: admin reads live under `permission:admission.view` (route group); `AdmissionController` is separate from `PublicAdmissionController` (3.3). Branch isolation needs **no** manual clause — `AdmissionApplication` uses `BelongsToBranch`, so list excludes and `{admission}` route-model binding 404s out-of-branch automatically. Judgment calls the ticket left open: (a) list defaults `status=pending` in the **service** (not the Form Request) and orders `created_at desc`; the contract's `submitted_at` maps to `created_at` (ISO-8601); (b) the date range filters `from`/`to` are inclusive `whereDate` on `created_at`; (c) detail's `reviewed_by` is rendered as a nested `{id,name}` (the reviewer user) or `null` — not the raw FK — to match the contract's `null` for pending while staying useful once 3.5 stamps a reviewer; (d) two resources split the shapes — compact `AdmissionListResource` (eager-loads `desiredClass`) vs full `AdmissionDetailResource` (eager-loads `desiredClass`/`previousEducations`/`reviewer`/`media`); `documents` maps medialibrary `documents` collection to `[{name:file_name,url}]`, `photo_url` via a new `AdmissionApplication::photoUrl()` mirroring Teacher. N+1 guard runs the index under `Model::shouldBeStrict()`.
- 2026-06-13 — Task 3.5 (surprise — spec vs. schema conflict): the ticket says the student login takes `phone = father_mobile`, but a father/guardian parent account's contact is *also* `father_mobile`, and `users.phone` is a global unique index — both logins can't hold it. Resolution: the **parent claims the shared number** (the parent is the human who owns it and the credential job/feature exists so they can log in), and the **student's login phone is set null** when it would collide. With no parent account (the common case + the happy-path test) the student keeps `father_mobile` exactly as specced. The parent's contact phone is still stored on the `ParentProfile` row regardless. Parent `name`/`phone` are derived from the relation: `mother` → `mother_name_en` + `mother_mobile` (falls back to `father_mobile` if absent); `father`/`guardian` → `father_name_en` + `father_mobile`.
- 2026-06-13 — Task 3.5 (surprise): students/parents have **null email**, but the reused `SendCredentials` job (2.2) mails to `$user->email` and uses it as the credential identifier → `TypeError`/null-recipient when the queue actually runs it (only hidden under `Queue::fake`). Since SMS delivery is out of scope, `SendCredentials::handle()` now **early-returns when `email` is null** (a no-op for phone-only accounts) rather than crash. The job is still dispatched afterCommit for student (+ parent) per the contract, so the dispatch assertions hold.
- 2026-06-13 — Task 3.5: `AdmissionService::approve()` runs one `DB::transaction` (student user → student row copying all bilingual/address fields + photo media via `$media->copy()` → active enrollment → optional parent user/profile/`parent_student` link → application `approved` + reviewer stamp); credential jobs dispatch `->afterCommit()`. The 409 "Application has already been reviewed." guard sits in the service (both approve + reject), so a rejected application can never be approved later. `ApproveAdmissionRequest` validates `roll_no` unique within `(session_id, class_id, section_id)` and `admission_no` unique on `students` (→ 422); the class is resolved branch-scoped and the section verified to belong to it in `after()` (→ 422 `errors.section_id`). `admission_no` auto-generates from the **session's** year (`session.start_date->year`) when absent, via `AdmissionNoGenerator` called inside the transaction. The approval response student block is `ApprovedStudentResource` (enrollment relations preloaded by the service, no lazy loads). Routes `POST /admissions/{admission}/approve|reject` live under `permission:admission.approve`; out-of-branch `{admission}` 404s via `BranchScope` binding.
- 2026-06-13 — Task 4.1: `StudentPolicy::view` is `user.can('student.view') || user.id === student.user_id` (super_admin bypasses via `Gate::before`); registered explicitly with `Gate::policy()` in `AuthServiceProvider`. The ticket wants **404, not 403** when a student requests another student — but the policy returning `false` yields 403 via `authorize()`, and an in-branch peer is never hidden by `BranchScope`. Resolved at the HTTP boundary: the `show` route carries **no** `permission` middleware (so students can reach their own record), and the controller does `if ($request->user()->cannot('view', $student)) abort(404)` to hide existence. The reusable bool policy stays clean for attendance/results/fees to consume; the 404-vs-403 choice lives in each controller. List/update/status/photo keep `permission:student.view|student.update` middleware, so a student hits 403 on the list (correct per criteria). admission_no **and** birth_reg_no are `prohibited` in `UpdateStudentRequest` (explicit 422). `tc` status is blocked in `UpdateStudentStatusRequest` via `Rule::in([active,inactive])` with a custom message ("Use the TC module…"). List display class/section/roll come from the eager-loaded `currentEnrollment` (3.2's is_current-session hasOne); filters `class_id/section_id/session_id` match via `whereHas('enrollments')` so a student filtered by a past session still shows their current-session row. N+1 guarded with `Model::preventLazyLoading()` in the list test (no global `shouldBeStrict` set in this project).
- 2026-06-13 — Task 4.2: `relation` stays a plain string column validated `Rule::in([father,mother,guardian])` (no enum — 3.5 already writes those literals; not worth a backed enum for three values used in two places). `ParentProfile::isLinkedTo(int)` is the project-wide gate (`students()->whereKey()->exists()`); since `students()` carries `BranchScope`, a cross-branch id can never report as linked. Branch validity of `student_ids`/`student_id` is enforced in the Form Requests' `withValidator()` by counting through the **branch-scoped** `Student` model (not `Rule::exists`, which runs a raw query and would leak/accept foreign-branch rows) — so a foreign-branch or non-existent id is an indistinguishable 422 (`errors.student_ids`/`errors.student_id`). Create mirrors `TeacherService`: `users` (parent role, creator's branch via `BelongsToBranch`, random password) + `parents` + pivot attach in one `DB::transaction`, `SendCredentials(...,'Parent')` dispatched `->afterCommit()`; parents may have a **null email** (phone-only login), and the 3.5 decision that `SendCredentials::handle()` no-ops on null email already covers them.
- 2026-06-13 — Task 4.2: `POST /parents/{id}/students` duplicate link → 409 (`abort(409)` in `ParentService::linkStudent`); `DELETE /parents/{id}/students/{student}` not-linked → 404. Both `{parent}` (ParentProfile) and `{student}` route bindings carry `BelongsToBranch`, so an out-of-branch parent/student 404s at binding — meaning the unlink 404 covers "valid-but-unlinked", "non-existent", and "foreign-branch" uniformly. `GET /me/students` sits outside the `permission:parent.manage` group (parents hold zero staff permissions per RoleSeeder); it's role-gated in the controller (`$user->hasRole('parent')` else 403) — the one sanctioned role-name check, since the endpoint is intrinsically the parent role and has no permission to key off. Returns the compact `LinkedStudentResource` (id/name_en/class/section/photo_url) shared with the parent payload's nested `students`.
- 2026-06-13 — Task 4.3 (surprise): the contract requires a **linked parent → 200**, but 4.1's `StudentPolicy::view` only granted staff (`student.view`) + self (`user.id === student.user_id`) — parents hold no staff permissions and aren't the student, so they'd have 404'd. Extended `StudentPolicy::view` with a third branch: a linked parent (`ParentProfile::where('user_id', …)->first()?->isLinkedTo($student->id)`, reusing 4.2's project-wide gate) may view. This also lifts the parent into the profile `show` endpoint (same policy) consistently. `EnrollmentResource` flattens session/class/section to their **names** (strings) per the contract example, unlike `StudentResource`'s nested `{id,name}` enrollment summary. The `students/{student}/enrollments` route carries no `permission` middleware (policy-guarded in the controller with `abort(404)` on denial — the same 404-not-403 existence-hiding as `show`); history is newest-first via `StudentService::enrollmentHistory` (eager-loads session/class/section, N+1 guarded under `preventLazyLoading`).
- 2026-06-13 — Task 5.1: `student_attendances` created with `enrollment_id` + `recorded_by` as `restrictOnDelete` FKs, `unique(enrollment_id, date)` and an index on `date` per schema. `StudentAttendance` carries **no** `branch_id` (derives through `enrollment.student` per schema §41); for the read-only sheet, branch isolation comes for free — the section is resolved branch-scoped in the Form Request, so all its enrollments are in-branch and the attendance `whereIn(enrollment_id)` can't reach foreign rows. No global scope on the model yet (5.2's bulk save will revisit). `AttendanceStatus` enum = present/absent/late/leave; a **missing** row (not an enum case) is the "not yet taken" `null`.
- 2026-06-13 — Task 5.1: "active enrollments of the section" implemented as `enrollments.status = active`, which is exactly what excludes TC (and promoted/failed) from the roster — promotion/TC close the enrollment, so the active filter is the single query scope the invariant calls for (no per-student-status check needed; the required TC test passes via the `tc` enrollment status). Roster is two queries (enrollments with `student.media`, then the day's `student_attendances` keyed by `enrollment_id`) — no N+1, asserted under `Model::preventLazyLoading()`.
- 2026-06-13 — Task 5.1: contract `data` includes both `class` and `section` names (the ticket's JSON shows `class`, though `api/student-attendance.md` line 14 lists only `section`) — followed the ticket. `GET /attendance/sheet` sits under `permission:attendance.create` (admin + teacher hold it); `class_id`/`section_id` required (missing → 422), section-not-in-class validated branch-scoped in `AttendanceSheetRequest::after()` → 422 `errors.section_id` (mirrors `ApproveAdmissionRequest`); `date` defaults to today. The composite payload is shaped by `AttendanceSheetResource` wrapping the service's array.
- 2026-06-13 — Task 5.2: the 5.1-deferred branch scope for `student_attendances` landed as a sibling `App\Models\Concerns\BelongsToBranchThroughEnrollment` trait (mirrors `BelongsToBranchThroughClass`): its global scope adds `whereHas('enrollment.student')`, and since `Student` carries `BranchScope`, an out-of-branch attendance row is model-not-found at route binding — so `PUT /attendance/{id}` 404s for both unknown ids and foreign-branch records uniformly. The 5.1 read-side sheet (which fetches by explicit `whereIn(enrollment_id)`) is unaffected.
- 2026-06-13 — Task 5.2: "teacher" for the assignment rule = a user with a `Teacher` profile (`Teacher::where('user_id', …)`, branch-scoped); such a user must hold a `teacher_assignments` row for the section's `class_id` (class-level, not section/subject-specific) or `saveBulk` aborts 403 "You are not assigned to this class". Non-teacher staff holding `attendance.create` (admins) have no `Teacher` profile, so the check is skipped — no role-name check needed. The 403 lives in `AttendanceService` (not a Form Request, since it isn't a validation error); structural validation (branch-scoped section, non-future date via date-string compare, active-in-section enrollments keyed `errors.records.N.enrollment_id`) lives in `StoreAttendanceRequest::after()`. "enrollment not in section / TC / inactive" collapse to one check: membership in the section's `status=active` enrollment id set.
- 2026-06-13 — Task 5.2: bulk save is a single `StudentAttendance::upsert($chunk, ['enrollment_id','date'], ['status','recorded_by','updated_at'])` per 500-row chunk; `created_at`/`updated_at` are set in the row payloads (upsert doesn't manage timestamps) and `created_at` is omitted from the update column list so re-posts preserve it. `recorded_by` stamped from the caller. Routes: `POST /attendance` under `attendance.create`, `GET /attendance` (paginated browse) under `attendance.view`, `PUT /attendance/{attendance}` under `attendance.update` — the `teacher` role holds create/view but **not** update (admin-only correction), per the existing RoleSeeder bundles. Test note: the Task 2.1 sanctum guard-cache gotcha recurs — `$this->app['auth']->forgetGuards()` is needed between the teacher (403) and admin (200) requests in one test.
- 2026-06-13 — Task 5.3: the ticket specs the monthly-read policy as `attendance.view` OR self OR linked parent — **not** 4.1's `StudentPolicy::view` (which keys off `student.view`). Added a sibling `StudentPolicy::viewAttendance` ability (same self/linked-parent branches, `attendance.view` permission) rather than overload `view`, so a teacher holding only attendance permissions can read sheets without gaining profile access. Routes `GET /students/{student}/attendance` + `GET /me/attendance` carry **no** permission middleware: `studentMonthly` does `cannot('viewAttendance') → abort(404)` (existence-hiding like the 4.1 profile reads), and out-of-branch `{student}` already 404s via BranchScope binding; `meMonthly` resolves the caller's `Student` by `user_id` and `abort(403)` when none (the non-student case), mirroring `/me/students`. Summary is one SQL aggregate (`SUM(CASE WHEN status = ? …)` with bound enum values — no interpolation) returning present/absent/late/leave + `working_days` (= COUNT of recorded rows); the day list is a second ordered query. Aggregation spans **all** the student's enrollments filtered by month/year, so a mid-month promotion's split rows still total correctly.
- 2026-06-13 — Task 5.3 (test gotcha, extends the 2.1 sanctum guard-cache note): `forgetGuards()` between authenticated requests is necessary but **not sufficient** when the test creates a branch-scoped model (`Student`/`ParentProfile`) *after* the reset — `BelongsToBranch::creating` calls `auth()->user()`, which re-resolves the sanctum guard off the leftover request bearer token and re-caches the *previous* user. Fix: create all actors + tokens up front, then run the request sequence with `forgetGuards()` immediately before each `withToken(...)` call.
- 2026-06-13 — Task 6.1: `checkin_ip_whitelists` uses `BelongsToBranch` (own `branch_id`), so list/binding isolation and create-stamping come for free — no manual branch clauses, and out-of-branch `{checkinIp}` PUT/DELETE 404 via `BranchScope` route binding. Branch handling mirrors the class pattern: super admins must send `branch_id` (validated `exists`), everyone else's `branch_id` input is `exclude`d and stamped from the caller. `teacher_attendances.corrected_by` FK is `nullOnDelete` (nullable audit pointer → users, same call as 3.1's `reviewed_by`); `teacher_id` is `restrictOnDelete`. Status column got `App\Enums\TeacherAttendanceStatus` (present/late/absent/leave) cast on the model now (CLAUDE.md requires an enum per status column) even though check-in writes land in 6.2.
- 2026-06-13 — Task 6.1: IP/CIDR validation is a reusable `App\Rules\IpOrCidr` (Laravel's `ip` rule rejects CIDR) — plain `filter_var(FILTER_VALIDATE_IP)` for exact addresses, and for `a.b.c.d/n` it validates the address family then bounds the prefix (0–32 IPv4, 0–128 IPv6). `WhitelistService::activeFor(Branch)` caches the branch's active entries under `checkin.whitelist.active.{branchId}` (1h TTL, queried `withoutGlobalScope(BranchScope)` so it works in the unauthenticated check-in context of 6.2); every create/update/delete forgets that branch's key. Cache invalidation is proven by a read→write→read test (priming empty, API create, then toggle-inactive drops it from the active set).
- 2026-06-13 — Task 6.2: check-in/check-out land under a new `role:teacher` middleware (added `'role' => Spatie\...\RoleMiddleware` alias in `bootstrap/app.php` — first role-name middleware in the project; non-teacher → spatie `UnauthorizedException` → existing 403 envelope). `CheckinService` resolves the request IP against `WhitelistService::activeFor(teacher->branch)` via a reusable `App\Support\IpMatcher` (exact match on normalized `inet_pton` form for IPv4+IPv6; IPv4 CIDR containment by 32-bit bitmask; **IPv6 CIDR never matches** per ticket's "exact-only acceptable"). IP mismatch → 403 `"Check-in is not permitted from this network"` (asserted `assertExactJson` — no whitelist details leak); the controller resolves the teacher via `Teacher::where('user_id', …)->firstOrFail()` (branch-scoped). Late vs present from `App\Support\SettingsRepository::teacherLateThreshold()` — a **stub** reading `config('attendance.teacher_late_threshold', '09:00')` (new `config/attendance.php`), swapped to the settings table in 14.1. `late` when `now() > today@threshold`. Daily uniqueness enforced in-service (today-record lookup → 409 `"Already checked in today"`); checkout → 409 `"Not checked in"` when absent, 409 `"Already checked out today"` on repeat. `TeacherAttendanceResource` returns id/date/check_in_at/check_out_at/status.
- 2026-06-13 — Task 6.3: the 6.1/6.2-deferred branch scope for `teacher_attendances` (no own `branch_id`, derives through the teacher) landed as a sibling `App\Models\Concerns\BelongsToBranchThroughTeacher` trait (mirrors `BelongsToBranchThroughEnrollment`/`…ThroughClass`): global scope adds `whereHas('teacher')`, and since `Teacher` carries `BranchScope`, an out-of-branch record is model-not-found — so browse excludes it and `PUT /teacher-attendance/{id}` 404s for unknown ids and foreign-branch rows uniformly. The 6.2 check-in/out read path (explicit `teacher_id`/date lookups) is unaffected. Admin routes: `GET /teacher-attendance` under `teacher_attendance.view`, `PUT /teacher-attendance/{teacherAttendance}` under `teacher_attendance.manage`; `GET /me/teacher-attendance` joins the existing `role:teacher` group (non-teacher → 403 from middleware, no controller check needed). Time-order validation (check-out must be **after** check-in) lives in `UpdateTeacherAttendanceRequest::after()`, resolving each side from the request when present else the stored record (so a one-sided correction is still checked) → 422 `errors.check_out_at`; `correct()` always stamps `corrected_by` from the caller. `/me` summary is one SQL aggregate (present/late/absent/leave, bound enum values) + an ordered records query — no `working_days` here (teacher attendance has no concept of it). `TeacherAttendanceResource` gained `teacher` + `corrected_by` as `whenLoaded` nested `{id,name}`, so the 6.2 check-in/out responses (relations not loaded) keep their existing shape.
- 2026-06-14 — Task 7.1: `grading_scales` is a **single global scale** — no `branch_id`, no `BelongsToBranch` (schema §322 carries no branch column; the scale drives result math identically across branches). `GET /grading-scales` is open to any authenticated user (cached, no permission middleware); `PUT /grading-scales` is `permission:setting.manage`, which **no role bundle holds** (per the 1.2 decision admin did not get `setting.manage`) — so today only super admin can replace the scale (Gate::before bypass), exactly as the contract's "setting.manage" gate implies. One service `GradeResolver` owns read+resolve+replace: `all()` caches the ordered scale under the global key `grading.scale` (1h TTL, ordered `min_marks` desc, A+→F — the order GET returns); `replace()` runs a `delete()`+bulk `insert()` in one transaction then `forget()`s the key; `resolve(marks)` picks the **first band whose `min_marks` the marks reach** walking highest-first (not a `min ≤ x ≤ max` between-check) so fractional marks that fall between two integer boundaries — e.g. 79.5 — still resolve (→ A) instead of hitting a phantom gap. `grade_point` is cast `decimal:2`, so it serializes as the string `"5.00"` (matches the contract's visual `5.00`); resolver returns it as that string for the marks snapshot in 7.3.
- 2026-06-14 — Task 7.1: full-replace validation lives in `UpdateGradingScaleRequest::after()`, first-violation-wins so the top-level envelope `message` is the specific failure (the `ValidationException` render closure surfaces `getMessage()` = first error). Exact messages: gap or non-0/100 coverage → `"Scale must cover 0–100 without gaps"`; overlap → `"Scale ranges must not overlap"`; zero **or** multiple fail rows → `"Scale must have exactly one failing grade"`; non-descending points → `"Grade points must descend as marks fall"`; per-band `min > max` → keyed `scale.N.min_marks`. Coverage/overlap checked in one ascending sweep (first floor must be 0, last ceiling 100, each next floor == prev ceiling + 1; `<` ⇒ overlap, `>` ⇒ gap). `grade_point` bounded `between:0,9.99` (column is `DECIMAL(3,2)`), not 0–5, to avoid baking the default scale's range into the validator.
- 2026-06-14 — Task 7.2: `exams` carries its own `branch_id` (schema §334) so `Exam` uses `BelongsToBranch`, but the POST contract has **no** `branch_id` field — rather than make super admins send one (the class/checkin pattern), `branch_id` is **derived from the class** in `ExamService::create` (`SchoolClass::findOrFail($class_id)->branch_id`). A class belongs to exactly one branch, so this is correct for everyone: for non-super-admins `BelongsToBranch::creating` re-stamps the same value; for super admins (scope bypassed, no own branch) it's the only source. `class_id` is validated through the branch-scoped `SchoolClass` model in `StoreExamRequest::after()` (out-of-branch → 422 `errors.class_id`, not a leak), mirroring `TeacherAssignmentRequest`. Duplicate (session,class,type) → 422 keyed on `errors.type` with the contract's exact message "This exam already exists for the class" (the `ValidationException` render closure surfaces it as the top-level `message`); also backed by a DB `unique(session_id, class_id, type)` index.
- 2026-06-14 — Task 7.2: the published-freeze (409) vs status-regression (422) ordering is handled in `UpdateExamRequest::after()` — it `abort(409, 'Published exams cannot be modified')` **first** (a published exam's status outranks any allowed target, so the regression check would otherwise mis-fire as 422), then rejects backward `status` moves via `ExamStatus::rank()`. `session_id`/`class_id`/`type` are `prohibited` (422) on update per the immutability rule. The update endpoint's `status` enum **excludes** `published` (`Rule::enum(...)->except([Published])`) — publishing is the dedicated 8.1 flow, so the generic update can advance only to upcoming/ongoing/completed. `exams.session_id` uses `restrictOnDelete`, satisfying the 1.4-anticipated restrict FK to `academic_sessions` (the synthetic-table test there can now point at a real table).
- 2026-06-13 — Task 1.6: subject delete-in-use 409 depends on the future `marks` table — **Task 7.3 must declare a restrict FK to `subjects`**; tested via a synthetic restrict-FK table, mirroring 1.4/1.5. Class delete with subjects already 409s for real via the `subjects.class_id` restrict FK (asserted at DB level in the test).
- 2026-06-14 — Task 7.3: `marks` carries **no** `branch_id` (schema §41) — it reuses `BelongsToBranchThroughEnrollment` (the same trait `student_attendances` uses), so the `{exam}`-nested reads/writes isolate via the enrollment→student chain and the `Mark` model needs no manual branch clause. The 1.6-anticipated restrict FK to `subjects` landed here (`subject_id` `restrictOnDelete`), so subject delete-in-use now 409s for real via marks, not just the synthetic table.
- 2026-06-14 — Task 7.3: the published-freeze **409** is raised by `StoreMarksRequest::after()` via `abort_if(... Published ...)` at the **top** of the closure (before any 422 range/subject checks), so a published exam is frozen regardless of payload validity — mirroring `UpdateExamRequest`'s published-first ordering. The teacher-assignment check is **subject-level** (not class-level like attendance): a user with a `Teacher` profile must hold a `teacher_assignments` row for the posted `subject_id`, else `MarkService` aborts **403** "You are not assigned to this subject"; non-teacher staff holding `marks.entry` bypass it.
- 2026-06-14 — Task 7.3 (surprise — the ticket's "admin ok" test): per the 1.2 bundle decision **admin holds `marks.view` but NOT `marks.entry`** (teachers enter marks). So the only non-teacher holder of `marks.entry` is **super_admin** (via `Gate::before` bypass), which also has no `Teacher` profile — that is the "non-teacher staff with permission bypass" case, and the tests use a super_admin token for it. A plain admin posting marks correctly 403s at the `permission:marks.entry` middleware (covered by a test).
- 2026-06-14 — Task 8.1: `exam_results` carries **no** `branch_id` (schema §41) — reuses `BelongsToBranchThroughEnrollment` (same trait as `marks`/`student_attendances`), so generate/publish/browse isolate via the enrollment→student chain and `{exam}` binding 404s out-of-branch via `BranchScope`. `ResultService` (constructor-injects `GradeResolver`) owns the engine: generate iterates the exam's **active** enrollments (`class_id` + `session_id` of the exam, `status=active` — so TC/promoted are excluded for free) and requires a mark for **every** subject of the class (`subjects.class_id = exam.class_id`); any enrollment missing one (including one with zero marks → all subjects listed) is skipped and reported `{enrollment_id, missing_subjects:[names]}`, never written. Complete rows are bulk-`upsert`ed (chunks of 500) on the unique `(exam_id, enrollment_id)` so regenerate is idempotent and reflects changed marks.
- 2026-06-14 — Task 8.1: per-exam GPA = average of the marks' snapshotted `grade_point`s, `round(.., 2)` (PHP half-away-from-zero = the ticket's "round half up" for non-negative GPAs). **Overall grade** is mapped from the GPA via the scale's grade points (highest band whose `grade_point` the GPA reaches) — e.g. GPA 3.50 → `A-` (band point 3.50), **not** B; verified in tests. The **any-F override**: a subject is "failed" when its stored grade is in the scale's failing-grade set (`GradeResolver::all()->where('is_fail', true)->pluck('grade')`, not a `grade_point == 0` check), and any failed subject forces `is_passed = false` and overall grade = the failing grade regardless of GPA. `total_marks`/`gpa` cast `decimal:2`, so they serialize as the contract's strings (`"297.00"`, `"4.13"`); `is_passed` is a real bool in JSON.
- 2026-06-14 — Task 8.1: `generate` 422s `"No marks entered for this exam"` only when the exam has **zero** mark rows at all (an exam with some marks but incomplete enrollments returns 200 with everyone in `skipped`). Both generate and publish raise **409** when `exam.status === Published` (publish freezes by stamping `published_at` on all rows + setting exam status `Published` in one transaction; re-publish and post-publish regenerate both 409). All three endpoints sit under `auth:sanctum`: generate/publish need `result.generate` (admin + super_admin only — teacher holds just `result.view`), browse needs `result.view`; browse is paginated (default 15, max 100), ordered `gpa` desc, filters `section_id` + `is_passed` (the latter coerced via `$request->boolean` in the controller, only applied when present so `is_passed=0` filters to failures).
- 2026-06-14 — Task 8.2: `annual_results` carries **no** `branch_id` (schema §41) — reuses `BelongsToBranchThroughEnrollment` like `exam_results`/`marks`, so generate/publish isolate via the enrollment→student chain. Generate/publish take `{session_id, class_id}` in the body (no route-bound model), so branch isolation rides on `AnnualResultRequest` resolving `class_id` through the branch-scoped `SchoolClass` (out-of-branch/unknown class → 422 `errors.class_id` = the "unknown tuple → 422" case); `session_id` is a plain `exists` rule (sessions are global). The same request class backs both endpoints. Exams are looked up via the branch-scoped `Exam` model, so an out-of-branch tuple resolves to an empty exam set → the all-published guard fails (409) rather than leaking.
- 2026-06-14 — Task 8.2 (rounding edge — the explicit 3.555 → 3.56 criterion): `round(0.25·S1 + 0.25·S2 + 0.50·Final, 2)` is **wrong** — accumulating the weights as floats yields 3.5549999… for the 3.555 edge and rounds **down** to 3.55 (verified: `round(0.25*3.55+0.25*3.55+0.50*3.56,2) === 3.55`). Since the three GPAs are already 2-dp values, `AnnualResultService::weightedGpa` computes in **integer hundredths**: annual = (S1ₕ + S2ₕ + 2·Finalₕ) / 4, rounded half-up to the nearest hundredth via `intdiv(numerator + 2, 4)`. Exact for all edges. A unit test pins 3.555 → 3.56.
- 2026-06-14 — Task 8.2: `is_passed` and `grade` are decoupled per the ticket's literal wording ("grade via scale grade-point mapping, is_passed rule"): grade is **always** the band the annual GPA maps to (`gradeForGpa`, highest band whose grade point the GPA reaches), while `is_passed = final_result.is_passed && grade ∉ failGrades`. So a failed final with a high weighted GPA stores e.g. grade A but `is_passed=false` (no F-override on the grade, unlike per-exam result rows where any failing subject rewrites the grade to F). `skipped` reason is the **first** missing exam type in S1→S2→Final order, formatted `"missing {type} result"` (e.g. `"missing first_semester result"`) per the contract example. Active-enrollment filter excludes TC/promoted/failed for free (tested). Idempotency-until-publish guard checks for any published `annual_results` row among the tuple's enrollments; publish stamps `published_at` in a transaction. Browse/student reads deferred to 8.3.
- 2026-06-14 — Task 7.3: `obtained_marks` is cast `decimal:2` (DB `DECIMAL(5,2)`) but the contract types the **sheet/browse** `obtained_marks` as `number`, so the resources cast it to `(float)` on output (78.5, not "78.50"); marks are not money, so the decimal-string-in-JSON money rule does not apply. Grade + grade point are resolved from `GradeResolver` at entry and stored; the snapshot-immunity test proves a post-entry scale replacement leaves stored grade/point unchanged. Bulk save is one `Mark::upsert` per 500-row chunk on the `(exam_id, enrollment_id, subject_id)` unique key (idempotent re-entry), `entered_by` stamped from the caller. The entry-sheet `exam` block is a compact `{id, name}`.
- 2026-06-14 — Task 8.3: the **result bundle** `obtained_marks` (and all GPAs/grade points) serialize as decimal **strings** (`"78.50"`, `"4.00"`) — this contract's example explicitly shows quoted values, the opposite of 7.3's marks sheet/browse `number` choice, so the bundle reads the decimal-cast values straight through (no `(float)`). One `ResultBundleResource` shapes the array `ResultService::bundle()` returns; the bundle eager-loads enrollment.student/schoolClass/section, exam_results.exam, and marks.subject (no N+1).
- 2026-06-14 — Task 8.3: `Enrollment` has **no** branch scope (per the 3.2 decision — isolation rides on the student relation), so the result reads scope every enrollment lookup through `whereHas('student')` (Student carries `BranchScope`): `searchEnrollment` (both styles), `resolveEnrollment` (the `/enrollments/{id}/results` binding — uses `{id}` + manual `findOrFail`, **not** route-model binding, since the model isn't scoped), and the `me/results` student/parent reads all 404 out-of-branch this way. Admission-no search resolves to the student's **current**-session enrollment, falling back to their latest (`orderByDesc('session_id')`); `me/results?session_id=` selects the named session else current/latest.
- 2026-06-14 — Task 8.3: published-vs-preview is keyed on `result.view`, not a role check — staff (anyone with `result.view`, incl. super_admin via `Gate::before`) get `publishedOnly = false` (all results, each flagged `published`), students/parents get `publishedOnly = true` (unpublished per-exam results omitted, `annual` → `null` until published). The new `StudentPolicy::viewResults` mirrors `viewAttendance` (result.view OR self OR linked parent); `/enrollments/{id}/results` carries **no** permission middleware (policy-gated in the controller with `abort(404)` existence-hiding, like the 4.x student reads) so students/parents — who hold no `result.view` — can reach their own. `/me/results` resolves the caller's `Student` first (student → own, any `student_id` ignored), else their `ParentProfile` (parent → `student_id` **must** be linked, else 404; neither student nor parent → 403). `/results/search` keeps `permission:result.view` middleware (staff/teacher only).
- 2026-06-14 — Task 9.1: the ticket says "join **active** enrollments × annual_results" yet the reason taxonomy and required test demand `tc` appear — irreconcilable if the cohort is strictly active (TC closes the enrollment to status `tc`). Resolved by defining the preview cohort as enrollments with status `active` **or** `tc` (the only open-vs-this-session states pre-promotion); `tc` rows short-circuit to `not_eligible` reason `tc`, active rows are split passed/failed/no_result against **published** annual results. Closed `promoted`/`failed` rows (set by 9.2) are excluded so a re-run preview is clean. `promotions` migration created per schema with a `PromotionType` enum (bulk|individual) cast on a new `Promotion` model (no branch trait — derives through the student) + `PromotionFactory`; the model/enum are unused by 9.1's read-only preview but seed 9.2/9.3.
- 2026-06-14 — Task 9.1: the published guard is "**any** published annual_result exists among the cohort's enrollments → proceed, else 409 'Publish annual results first'" — annual publication is all-or-nothing per (session,class) (8.2 stamps every row at once), so an existence check is sufficient and also 409s an entirely empty/un-generated cohort. `annual_gpa` serializes as the decimal **string** `"4.59"` (read straight off the `decimal:2` cast), matching the contract; `roll_no` comes from the enrollment. Next class = branch-scoped `numeric_level + 1` lookup (top class → `to_class: null`). Preview is 4 queries (class, enrollments+student eager, published annual_results, next class) — no N+1. `GET /promotions/preview` sits under `permission:promotion.execute`; `PreviewPromotionRequest` mirrors `AnnualResultRequest` (required `session_id`/`class_id`, branch-scoped class existence → 422).
- 2026-06-14 — Task 9.2: re-run guard plucks the cohort's `student_id`s for the source (session, class) across **all** statuses, then 409s if any already hold a `to_session_id` enrollment — checked first, before the publish guard, because a successful run closes the active source rows (an active-only check would miss a completed run and mis-fire as "publish first"). The 409s (publish-first, already-promoted) live in `PromotionService::bulk`; the 422s (same session via `different:from_session_id`, source class in-branch, target section belongs to next class `numeric_level+1`) live in `BulkPromotionRequest` per the project's 422-in-request / 409-in-service split. `to_class_id` for promoted rows is derived from the validated section's `class_id` (not re-resolved by numeric_level). Old enrollments closed with bulk `update` (promoted/failed), new ones bulk `insert`ed, logs bulk `insert`ed — all chunked at 500 in one `DB::transaction`; `to_enrollment_id` for promoted logs is re-fetched by `(student_id, to_session, to_class, to_section)` after insert, held logs carry null. Surprise: the task was found already fully implemented (service/controller/request/routes/9 tests) from a prior session but left unticked on the board — verified against spec and tests green, then ticked.
- 2026-06-14 — Task 9.3: `POST /promotions/individual` reuses the 9.2 close-old/create-new/log pipeline for one student (type `individual`), but not in bulk — a single `update`+`create`+`create` in one `DB::transaction`. The **source** enrollment is the student's current `active` row (latest by `session_id`, branch-scoped via `whereHas('student')`); none → **404** "Student has no active enrollment to promote". Guard order in the service: source-resolve (404) → already-in-target-session (**409** "Student is already enrolled in the target session") → pass/override gate. The pass gate requires a **published, passed** annual result on the source enrollment; otherwise the actor must hold `promotion.override` (checked via `$actor->cannot('promotion.override')`, so super_admin bypasses via Gate::before) else **403** "Student has not passed; override permission required". The 422s (in-branch student, section-belongs-to-target-class, duplicate roll) live in `IndividualPromotionRequest` per the 422-in-request / 4xx-in-service split; duplicate roll checks `(to_session_id, to_section_id, roll_no)` existence → `errors.roll_no`. The success `to_class_id` is taken **as given** (not derived by numeric_level+1 like bulk — individual is the explicit-target/override path).
- 2026-06-14 — Task 9.3: `GET /promotions` (history) sits under `permission:promotion.view` (its own route group — bulk/preview/individual are `promotion.execute`). Branch isolation rides on `whereHas('student')` (Student carries `BranchScope`), mirroring the 8.3 result-read decision (`Promotion`/`Enrollment` have no branch scope of their own). Filters `session_id`/`class_id` apply to the **source** (`fromEnrollment`) so a row is found by where the student came from; `type` filters bulk|individual. `PromotionResource` returns student `{id,name_en}` + `from.class`/`to.class` names (`to.class` null for a held-back row) + type + `promoted_at`; paginated (default 15, max 100) with the standard `meta`, ordered `promoted_at` then `id` desc. All relations eager-loaded (no N+1). Test gotcha: the 2.1 sanctum guard-cache recurs in `test_history_filters_by_type` — `forgetGuards()` before each `getJson` after the `promote()` call (which authenticated a different user).
- 2026-06-14 — Task 10.3: **first PDF task** — installed `barryvdh/laravel-dompdf ^3.1`; receipt streams via `Pdf::loadView('pdf.receipt', …)->stream(...)` (blade at `resources/views/pdf/receipt.blade.php`, DejaVu Sans for future Bangla). Created the schema-only `categories` + `incomes` migrations (per database-schema.md; CRUD deferred to 11.1/11.2) plus `payments`; `payments.receipt_no`/`transaction_id` are `unique()->nullable()` (set on success / for online), `collected_by` `nullOnDelete`, `incomes.payment_id` `unique` (one fee income per payment) `cascadeOnDelete`, `category_id` `nullOnDelete`. New enums `PaymentStatus` (pending/paid/failed/cancelled) + `PaymentMethod` (cash/sslcommerz). `Income` model is minimal (no branch trait yet — 11.2 adds scoping/CRUD); `Payment` uses `BelongsToBranch` so reads/`{id}` bindings + the receipt route isolate per branch (out-of-branch → 404).
- 2026-06-14 — Task 10.3: `PaymentService::settle()` is THE pipeline (one `DB::transaction`, reused verbatim by the 10.5 IPN): `lockForUpdate` the invoice → payment `paid` + `paid_at` + `receipt_no` → invoice `paid_amount` rolled up (paid when `>=` amount, else partial) → exactly one linked income row. **Money math is bc-string** (`bcadd`/`bcsub`/`bccomp` at scale 2) so 2dp decimal strings never round through floats. `receipt_no` = `RCPT-{branchCode}-{paymentId:06d}` (the example's id 41 → `RCPT-MP-000041` aligns id and seq; the auto-increment id is the race-safe per-branch sequence). `branch_id`/`created_by` on payment+income are set **explicitly** from the invoice/collector (not relying on `BelongsToBranch::creating`, which no-ops for super admins and the public IPN). Income title = `"Monthly fee {month}/{year} — {invoice_no}"`, date = `paid_at`.
- 2026-06-14 — Task 10.3: amount rules split per the project's 409-in-service / 422-in-request convention but both 422s for amount live in **`PaymentService`** (they're stateful — depend on the invoice's outstanding + the `partial_payment_enabled` setting), thrown as `ValidationException::withMessages(['amount' => …])`: partial disabled → must equal outstanding (`"Full payment of {outstanding} required"`), enabled → `0 < amount <= outstanding` (over → `"Amount may not exceed the outstanding {outstanding}"`), `<= 0` → `"Amount must be greater than zero"`. `LocalPaymentRequest` only shape-validates (`required|numeric|decimal:0,2`). Already-paid invoice → `abort(409, 'Invoice is already paid')` checked first. New `SettingsRepository::partialPaymentEnabled()` reads `config('fees.partial_payment_enabled', false)` (settings table in 14.1).
- 2026-06-14 — Task 10.3: routes `POST /invoices/{invoice}/payments/local` (`permission:fee.collect`, route-model-bound invoice → out-of-branch 404) and `GET /payments/{id}/receipt` (no permission middleware — authorizes via the existing `StudentPolicy::viewInvoices` on `payment->invoice->student`: staff `invoice.view`/self/linked parent, denial **and** non-paid payment both → 404 existence-hiding, mirroring `invoices.show`). `InvoiceResource.payments` now serializes the real `payments` relation (newest-first, loaded by `InvoiceService::find`) via `PaymentResource` instead of the 10.2 placeholder `[]`. `PaymentResource` nests `invoice {id,status,paid_amount}` (whenLoaded, no recursion) + `receipt_url`; rollback proven by inducing an `Income::creating` exception → 500 with zero rows persisted.
- 2026-06-14 — Task 10.3 (pre-existing flakiness noted, not fixed — out of scope): `IndividualPromotionTest::test_history_filters_by_type` (9.3) intermittently fails on a `school_classes (branch_id, numeric_level)` unique collision from `SchoolClassFactory`'s random `numeric_level`; reproduces with this task's changes stashed, so it's unrelated to 10.3.
- 2026-06-14 — Task 8.3: `SearchResultsRequest` enforces exactly one query style in `after()` — admission_no XOR the full (session, class, section, roll) coordinate set; mixing them, or an **incomplete** coordinate set (which counts as neither), is a 422 on `errors.admission_no`. No-match resolution → 404 (`firstOrFail`/`abort_if`).
- 2026-06-14 — Task 10.1: `fee_structures` carries its own `branch_id` (schema §407) so `FeeStructure` uses `BelongsToBranch` (auto-stamp + scope) — list/show isolate automatically and `{feeStructure}` PUT binds 404 out-of-branch via `BranchScope`. Mirrors the 7.2 exam pattern: the POST contract has **no** `branch_id` field, so `branch_id` is **derived from the class** in `FeeStructureService::create` (a class belongs to one branch — correct for super admins who carry no branch, and re-stamped identically by `BelongsToBranch::creating` for everyone else); `class_id` is validated through the branch-scoped `SchoolClass` in `StoreFeeStructureRequest::after()` (out-of-branch → 422 `errors.class_id`, not a leak). Duplicate (branch, session, class) → 422 keyed `errors.class_id` with the contract's exact message "Fee already defined for this class and session", backed by a DB `unique(branch_id, session_id, class_id)` index. Money: `monthly_fee` cast `decimal:2` (serializes as `"1600.00"`), validated `numeric|min:0|decimal:0,2` (negative → 422, 3dp → 422). PUT edits **monthly_fee only** — `session_id`/`class_id` `prohibited` (422); per the ticket the amount change affects only future invoice generation. **No DELETE route** (history matters) — verified by a test asserting 405. Routes under `permission:fee.manage`.
- 2026-06-14 — Task 10.2: `invoices` carries its own `branch_id` so `Invoice` uses `BelongsToBranch` — list/show/`{invoice}` isolate automatically (out-of-branch → 404). Generation (`InvoiceService::generate`) processes **active enrollments of active students** (`Enrollment::where(status, active)->whereHas('student', status active)`), which rides the branch-scoped `Student` relation: the manual endpoint generates only the caller's branch, the scheduler (no auth user → `BranchScope::bypassed`) generates **every** branch. Bulk `insert()` chunked at 500 sets `branch_id` explicitly per row (model events don't fire on bulk insert, and the console has no auth user to stamp from). **Idempotency** = DB `unique(student_id, month, year)`; the service pre-loads the period's already-invoiced student_ids into a skip set so re-runs report `skipped_existing` and create 0. **Amount is snapshotted** from `fee_structures.monthly_fee` at generation (a later fee edit never touches issued invoices; a later month copies the new amount) — proven by tests. `invoice_no` = `INV-{branch.code}-{yyyymm}-{seq}`, seq zero-padded 4 digits continuing the per-(branch,month,year) count; `due_date` = `SettingsRepository::invoiceDueDay()` (new `config/fees.php` default 10, day of the invoice month). TC/inactive students excluded via the student-status filter (not per-feature conditionals); classes lacking a fee structure are **reported** (`missing_fee_structure: [{class_id}]`) not invoiced (distinct from `skipped_existing`). Command `invoices:generate {month?} {year?}` defaults to the current month so the scheduler calls it argless; registered `Schedule::command(...)->monthlyOn(1, '00:00')` in `routes/console.php`. Reads mirror the 8.3 results pattern: `GET /invoices` staff-only (`invoice.view`), `GET /invoices/{id}` carries **no** permission middleware and authorizes via new `StudentPolicy::viewInvoices` (staff `invoice.view` / self / linked parent, denial → 404 hiding), `GET /me/invoices` student/parent self-service. **Gotcha:** `find()` must eager-load `student:id,name_en,user_id` — without `user_id` the self-check `$user->id === $student->user_id` silently fails and a student 404s on their own invoice. `status` is the new `InvoiceStatus` enum (unpaid|partial|paid). **Payments deferred to 10.3+:** `InvoiceResource.payments` emits `[]` for now (field present per contract; no `payments` relation/model created yet).
- 2026-06-14 — Task 10.4: gateway wrapped behind a slim `App\Contracts\PaymentGateway` (`createSession(Payment): GatewaySession{gatewayUrl}`, `validate(tranId, payload): bool`) — `SslCommerzGateway` (HTTP) is the default binding in `AppServiceProvider::register`, tests rebind `FakeGateway` via `$this->app->instance(...)`. The ticket's signature `createSession(payment)` carries **no** URL param: the SslCommerz impl builds its own success/fail/cancel/ipn callbacks via `url('/api/v1/payments/sslcommerz/*')` (those public routes don't exist until 10.5, so no `route()` helper — hardcoded paths). Credentials read through three new `SettingsRepository` methods (`sslcommerzStoreId/StorePassword/Sandbox`) backed by new `config/services.php` `sslcommerz` block (config fallback, settings table in 14.1). `validate()` + the HTTP bodies are implemented but **untested** (real sandbox is out of scope) — only `createSession` is exercised, through the fake.
- 2026-06-15 — Task 10.5: the IPN settlement lives in `PaymentService::settleFromIpn(payment, payload)` — **one** `DB::transaction` holding a `lockForUpdate` on the payment row across the whole flow (the ticket's concurrency requirement; `settle()` itself was left untouched so the 10.3 local pipeline is unchanged). It re-reads status under the lock: already `paid` → returns `['status' => 'already_processed']` (idempotent no-op, the critical replay path); gateway `validate()` false **or** the IPN's reported `amount` ≠ the pending payment amount (bccomp at scale 2) → payment `failed` + payload stored, returns `['status' => 'failed']`; valid → store `gateway_payload`, run the 10.3 `settle()` pipeline, queue the payer notification, returns `['status' => 'paid', 'receipt_no' => …]`. **Crucial:** the failed/invalid path returns a status descriptor rather than `abort(422)` *inside* the transaction — an abort there would roll back the `failed` write. The controller (`PaymentController::ipn`) maps `failed` → 422, otherwise 200; unknown `tran_id` is looked up and 422'd (logged via `Log::warning`) before the service is even called.
- 2026-06-15 — Task 10.5: IPN + landing routes are **public** (no auth) and sit in their own `payments/sslcommerz` group in `routes/api.php` — **not** under the `/public` prefix, because the callback URLs `SslCommerzGateway` builds (10.4) are hardcoded `url('/api/v1/payments/sslcommerz/{ipn,success,fail,cancel}')`; the group prefix had to match exactly. In the public context `BranchScope` is bypassed (no auth user), so the payment is found globally by `transaction_id`. The three landing routes collapse to one `landing` controller method on a single `{result}` route constrained `->whereIn('result', ['success','fail','cancel'])`; it only reports `{payment_id, status}` (no state change — asserted by a full `toArray()` snapshot-equality test before/after all three), unknown `tran_id` → 404 (`firstOrFail`).
- 2026-06-15 — Task 10.5: payer notification is a new queued `SendPaymentReceipt` job (carries the `Payment`; `tries=3`, `backoff=[60,300]`; `PaymentReceiptMail` + `mail/payment-receipt.blade.php`), dispatched `->afterCommit()` from `settleFromIpn`. Mirrors `SendCredentials`: **no-op when the payer's email is null** (students/parents are phone-only; SMS out of scope). `afterCommit` + `Queue::fake` fires under `RefreshDatabase` (same precedent as 2.1/2.2), confirmed by the valid-IPN test's `Queue::assertPushed` and the replay test's `assertNotPushed`. Phase 10 complete.
- 2026-06-14 — Task 10.4: `PaymentService::initOnline` creates the pending `sslcommerz` payment (status `pending`, `transaction_id` `TXN-{uuid}`, `collected_by` = the acting payer) **and commits it before** calling the gateway, so a gateway throw → `forceFill(status failed)->save()` + `abort(502, 'Payment gateway unavailable. Try again.')` leaves a persisted failed row (no transaction wrapping the create — settlement/income is 10.5's job, nothing to roll back here). Amount optional → defaults to outstanding, reuses 10.3's `assertAcceptableAmount` (same partial-toggle/422 rules). Route `POST /invoices/{invoice}/payments/online` carries **no** permission middleware (students/parents initiate too) — authorizes via new `StudentPolicy::payOnline` (staff `fee.collect` / self / linked parent), and the controller maps a denial to **404** (existence-hiding, mirroring receipt/show) rather than the policy's default 403. Response is a dedicated `OnlinePaymentInitResource` (`{payment_id, transaction_id, gateway_url}`), not `PaymentResource`. **Test gotcha (5.3 sanctum guard caching):** the parent-201/unrelated-404 test must build both tokens + the intruder Student up front — a branch-scoped model created after `forgetGuards()` re-caches the prior request's user and the intruder authenticates as the parent (got 201 instead of 404 until reordered).
- 2026-06-15 — Task 11.1: `categories` table already existed (schema-only from 10.3); this task added the model/CRUD on top. `Category` uses `BelongsToBranch` (own `branch_id`) so list/show isolate automatically and `{category}` PUT/DELETE bind 404 out-of-branch. New `CategoryType` enum (income|expense) cast on the model; invalid type → 422 via `Rule::enum` in store/update requests. Duplicate (branch, name, type) → 422 keyed `errors.name` ("Category already exists"), enforced app-level in the Form Requests' `after()` against the branch-scoped model (the DB `unique(branch_id, name, type)` index is the final guard) — same name is allowed across the two types (tested). **In-use 409 is app-level, not DB RESTRICT:** the spec said "rows keep `category_id` via RESTRICT" but the 10.3 `incomes` migration already shipped `category_id` as `nullOnDelete`, so `CategoryService::delete` instead `abort_if($category->incomes()->exists(), 409, 'Category is in use')` before deleting. Expenses don't exist yet (11.3) — the check covers `incomes()` only today; 11.3 should extend it (and add an `expenses()` relation). Permission gate is `permission:income.manage|expense.manage` (spatie OR-pipe) — accountant-only per the 1.2 bundle decision (admin holds neither), so tests use the `accountant` role. `CategorySeeder` seeds the ticket's defaults (income: Tuition Fee, Donation / expense: Salary, Utilities, Maintenance, Stationery) per branch via `updateOrCreate` (idempotent; 6×2 branches = 12 rows on fresh seed); runs unauthenticated so `BranchScope` is bypassed and explicit `branch_id` is honored.
- 2026-06-15 — Task 11.3: `expenses` is a direct mirror of 11.2's incomes **minus the system-row concept** — expenses have no `payment_id`, so every row is freely editable/deletable (no `isSystem()` guard, no `is_system` field, plain 200 on PUT/DELETE per the ticket). Migration matches schema (`item_name` VARCHAR(150), `amount` DECIMAL(12,2), `date` indexed, `description` TEXT null, `category_id` nullOnDelete, `created_by` restrict). `Expense` uses `BelongsToBranch` (own `branch_id` → list/show isolate, `{expense}` binds 404 out-of-branch); `created_by` stamped from `Auth::id()` in `ExpenseService`. Category-type guard inverts incomes': store/update Form Requests' `after()` require an **expense**-type in-branch category (`Category::whereKey(...)->where('type', Expense)->exists()`) → 422 `errors.category_id` (income category rejected). Same filters/sorts as incomes (`category_id`, inclusive `from`/`to`, `search` on `item_name`, sort `date|amount` default `date desc`). Gate `permission:expense.manage` only. **Resolved the 11.1 follow-up:** added `Category::expenses()` HasMany and extended `CategoryService::delete` in-use 409 to `incomes()->exists() || expenses()->exists()`. 11 tests in `ExpenseCrudTest`.
- 2026-06-15 — Task 11.4: `assets` register CRUD + `GET /assets/summary`. New `assets` migration per schema (`name` VARCHAR(150), `description` TEXT null, `value` DECIMAL(12,2), `purchase_date` DATE null, `status` VARCHAR(20) default `in_use` indexed, branch/created_by FKs). New `AssetStatus` enum (in_use|damaged|disposed) cast on `Asset`, which uses `BelongsToBranch` (own `branch_id` → list/show/summary isolate, `{asset}` PUT/DELETE bind 404 out-of-branch). `created_by` stamped from `Auth::id()`. **Gotcha:** the DB `default('in_use')` is **not** reflected on the model instance `create()` returns (DB-side default), so `AssetResource` blew up reading `status->value` on null — fixed by `$data['status'] ??= AssetStatus::InUse` in `AssetService::create` so the in-memory model carries it. Summary is a single `selectRaw('status, COUNT(*), SUM(value)')->groupBy('status')` query keyed by status; **`total_value` sums in_use + damaged only, disposed excluded** (ticket decision) — accumulated with `bcadd` (bcmath, already used in PaymentService) and emitted via `number_format(...,2,'.','')` as decimal strings; `count` and `by_status` cover all three statuses incl. disposed. Filters `status`/`search` (name LIKE), sorts `value|purchase_date` (default `purchase_date desc`). Gate `permission:asset.manage` only (accountant). `assets/summary` route registered before `assets/{asset}` (no GET-show route exists anyway). 13 tests in `AssetCrudTest` (incl. empty-register summary returning all-zero strings, branch-isolated summary). Phase 11 complete.
- 2026-06-15 — Task 11.2: `incomes` table already existed (schema-only from 10.3); this task added the model/CRUD on top. `Income` gained `BelongsToBranch` (own `branch_id`, list/show isolate automatically, `{income}` PUT/DELETE bind 404 out-of-branch), `HasFactory`, a `category()` belongsTo, and an `isSystem()` helper (`payment_id !== null`) that `IncomeResource` surfaces as `is_system`. Immutability of system-generated fee income is enforced in `IncomeService` (`abort_if($income->isSystem(), 403, 'System-generated income cannot be modified')`) on **both** update and delete — not a policy/middleware, since whether a row is editable depends on its own `payment_id`, not the caller. `created_by` is stamped server-side from `Auth::id()` in the service (never input); `branch_id` via the trait. Category-type guard: `category_id` is validated in the store/update Form Requests' `after()` against `Category::whereKey(...)->where('type', Income)->exists()` (BranchScope auto-applies, so an out-of-branch *or* expense category both fail) → 422 `errors.category_id`. amount `numeric|min:0` (negative → 422, zero allowed); future dates allowed (no rule, per ticket decision). List filters: `category_id`, inclusive `from`/`to` via `whereDate >=/<=`, `search` (title LIKE); sort restricted to `date|amount` (default `date desc`). Gate is `permission:income.manage` only (not the OR-pipe categories use) — incomes are income-side only. 14 tests in `IncomeCrudTest`.
- 2026-06-17 — Task 12.1: `GET /students/{id}/id-card` streams a CR80 portrait card via dompdf (`setPaper([0,0,153.01,242.65])`) — no table, rendered live. `IdCardService::render` requires `student.status === active` **and** an `active` enrollment in the **current** session (`whereHas session is_current`); either missing → 422 `Student has no active enrollment` (TC/inactive students fail the status check first, so the single message covers all three failure modes the contract lists). Out-of-branch `{student}` 404s via BranchScope route binding; route guarded by `permission:idcard.generate` (admin holds it per 1.2 bundle). Photo embedded by **local media path** (`getFirstMedia('photo')?->getPath()`, guarded by `is_file`) not URL — dompdf reads disk, not HTTP; missing photo renders a CSS `&#9787;` silhouette placeholder. **Surprise: `branches` has no `logo` column/media** (schema only has name/address/phone/email/code), so the ticket's "branch logo" is rendered as the branch **name band** only — a logo source would need a schema/media addition (defer to settings or a branches.logo migration if wanted). dompdf `stream()` returns a normal (non-streamed) Response with `Content-Disposition: inline; filename=idcard-{admission_no}.pdf`, so tests assert via `getContent()` (`%PDF` prefix), not `streamedContent()`. 4 tests in `IdCardSingleTest`.
- 2026-06-17 — Task 12.3: transfer certificates = issue/list/show/pdf + cross-module exclusion. New `transfer_certificates` table per schema (`student_id` **unique** → one TC per student, `tc_no` unique, `restrictOnDelete` FKs); `TransferCertificate` model uses `BelongsToBranch` (own `branch_id`, list/show/`{tc}` isolate + 404 out-of-branch) **and** `InteractsWithMedia` with a single-file `certificate` collection accepting `application/pdf` — **the project's one stored PDF** (legal record), persisted via `addMediaFromString($pdf->output())` (no streaming). `TcNoGenerator` mirrors `AdmissionNoGenerator` exactly (branch row `lockForUpdate` inside the issue transaction; format `TC-{branchCode}-{seq}`, seq zero-padded **4** digits per the contract's `TC-MP-0003`). `POST /students/{id}/tc` (`tc.issue`) → `TransferCertificateService::issue`: 409 (`Transfer certificate already issued`) **before** the transaction if `student->transferCertificate()->exists()`, else one `DB::transaction` creates the row, flips `students.status`→tc and **all active** enrollments→tc, then renders+attaches the PDF. **Atomicity proven by mocking `Pdf::shouldReceive('loadView')->andThrow(...)`** (the render is the last step) → 500 + full rollback (no row, statuses untouched). **Exclusion was already in place** in all three modules (no scope changes needed) — attendance sheet filters enrollment `active` (5.1), invoice generation filters active student+enrollment (10.2), promotion preview lists TC enrollments as `not_eligible/tc` and bulk only processes `active` (9.1); the task just **proves** it end-to-end (issue a TC via the endpoint, then assert the student drops from the roster, is skipped by `invoices/generate`, and is `not_eligible` in preview). Unpaid past invoices stay readable (no block by design — tested via `GET /invoices/{id}` after issue). `GET /tcs` (from/to on `issue_date`, search across tc_no+student name/admission_no, paginated+meta), `GET /tcs/{id}`, `GET /tcs/{id}/pdf` (download stored media; **missing file → `Log::error` + 500**, exercised by `@unlink`-ing the media file). `TransferCertificateResource` embeds `StudentListResource` and emits `pdf_url` = `route('v1.tcs.pdf', …, absolute:false)`. admin holds `tc.issue`/`tc.view` per the 1.2 bundle. **Bilingual-name caveat:** `pdf/tc.blade.php` prints both `name_bn`/`name_en` but uses the project-standard `DejaVu Sans` (no Bengali glyphs) — same pre-existing limitation as the receipt; a Bengali-capable font is a project-wide change, out of scope here. Out of scope per ticket: TC revocation/re-admission (open question). 12 tests in `TransferCertificateTest`. Phase 12 complete.
- 2026-06-17 — Task 12.2: batch ID cards = queued merged PDF + poll + download. New `id_card_batches` table with a **UUID primary key** (`HasUuids`, opaque since the id is exposed in poll/download URLs) — added a nullable **`error`** column beyond the ticket's listed columns so the poll endpoint can surface the `failed` message the contract requires. `IdCardBatchStatus` enum (processing|done|failed). `IdCardBatch` uses `BelongsToBranch` → `{batch}` route binding 404s out-of-branch (foreign-id case) automatically. `POST /id-cards/batch` (202) checks the eligible cohort **synchronously** in `IdCardService::queueBatch` (empty → 422 `No eligible students`) *before* creating the row/dispatching, so callers never poll a doomed batch. `BuildIdCardBatch` job (`tries=3`, backoff, `failed()` hook marks the row failed) calls `IdCardService::buildBatch`, which **chunks** eligible students (`chunkById(500)`) building card view-data, renders one merged `pdf.id-card-batch` (loops the shared partials with `page-break-after`), and `Storage::disk('local')->put('idcards/batches/{uuid}.pdf', …)`. **Key branch-scoping note:** the job runs with no auth, so `BranchScope` is inert — `eligibleStudents()` filters `students.branch_id` **explicitly** from `$batch->branch_id` (the documented console exception to the never-manual-branch_id rule); TC/inactive excluded by the active student+enrollment status filters. **Refactor:** extracted the 12.1 card markup/styles into `pdf/partials/id-card-body` + `id-card-styles`, included by both the single and batch views (single output unchanged, tests still green). Poll returns `{status}` + `url` (relative, `route(..., absolute:false)`) when done / `message` when failed; download 409s unless done **and** file present. **Gotcha:** api routes carry a `v1.` name prefix, so the download `route()` name is `v1.id-cards.batch.download`. Cleanup: `idcards:prune-batches {--days=7}` command (deletes file+row past retention, BranchScope inert → all branches) scheduled `dailyAt('01:00')`. **TC count test** asserts page count via `preg_match_all('~/Type\s*/Page(?![s/])~')` on the PDF bytes (page dicts are uncompressed) — 2 active → 2 pages, TC absent. 8 tests in `IdCardBatchTest`.
